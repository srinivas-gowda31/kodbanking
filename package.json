{
  "name": "kodbanking",
  "version": "1.0.0",
  "description": "Secure Digital Banking Platform - Vercel Deployment",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm start\"",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../api && npm install",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.1.0",
    "mysql2": "^3.6.5",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "cookie-parser": "^1.4.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "18",
    "npm": "9"
  }
}
